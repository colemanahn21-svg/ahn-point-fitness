import Foundation

// Template for WHOOP API credentials.
//
// 1. Create an app at https://developer.whoop.com to get a client ID + secret.
//    (Or skip WHOOP entirely and use Demo Mode in Settings.)
// 2. Copy this file into the app source tree as:
//        AhnPointFitness/Services/WhoopSecrets.swift
// 3. Fill in your credentials below. The real file is gitignored.

enum WhoopSecrets {
    static let clientID     = "YOUR_CLIENT_ID"
    static let clientSecret = "YOUR_CLIENT_SECRET"
}
